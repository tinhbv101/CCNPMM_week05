{{!-- <h1>{{post.title}}</h1>
<h2>{{post.content}}</h2>
{{#each post.comments}}
    <h3>User: {{user}}</h3>
    <h5>{{comment}}</h5>
{{/each}} --}}


<div class="row">
    <div class="bg-light col-12">
        <p class="col-12 text-center fw-b h2 bg-secondary mb-3 mt-3 text-light p-1">{{post.title}}</p>
        <p class="col-12 text-left fw-b h5 mb-3 p-1">{{post.content}}</p>
    </div>
</div>
<div>
    {{#each post.comments}}
    <div>
        <p>User: {{user}}</p>
        <p>{{comment}}</p>
    </div>
    {{/each}}
</div>
<div class="text-center mt-5">
    <a class="btn btn-primary btn-lg" href="/{{post.id}}/edit">Modify</a>

    <a class="btn btn-primary btn-lg" href="/{{post.id}}/comment">Comment</a>
</div>

